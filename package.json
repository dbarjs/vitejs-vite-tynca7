{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "purge": "npx purgecss --css ./css/** --content ./js/**/*.js ./dom/**/*.html --output ./build/css --keyframes"
  },
  "devDependencies": {
    "vite": "^5.2.8"
  }
}
